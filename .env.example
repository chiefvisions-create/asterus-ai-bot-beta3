# Database Configuration
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/asterus_ai

# ======================================
# Auth0 Authentication Configuration
# ======================================
# Auth0 is required for user login/logout functionality.
# All five variables below must be set for authentication to work.

# AUTH0_SECRET - Random secret for session encryption
# Generate a secure 32-byte random string using one of these commands:
#   - openssl rand -base64 32   (recommended)
#   - openssl rand -hex 32
# Keep this secret safe and never commit it to version control.
# For Railway deployments, set this as an environment variable.
AUTH0_SECRET=GENERATE_WITH_openssl_rand_base64_32

# AUTH0_BASE_URL - Your application's public URL
# Examples:
#   - Local development: http://localhost:5000
#   - Railway deployment: https://your-app.railway.app
# This URL is used for Auth0 callback redirects.
AUTH0_BASE_URL=http://localhost:5000

# AUTH0_ISSUER_BASE_URL - Your Auth0 tenant domain URL
# Format: https://your-tenant.auth0.com or https://your-tenant.us.auth0.com
# For this project's pre-configured tenant:
AUTH0_ISSUER_BASE_URL=https://dev-ejjwtoxn7b3krvga.us.auth0.com

# AUTH0_CLIENT_ID - Auth0 application client ID
# This is a public identifier for your Auth0 application.
# For this project's pre-configured application:
AUTH0_CLIENT_ID=ME9UbyrFx2l029rW8Ai9asSC4T62k2Ao

# AUTH0_CLIENT_SECRET - Auth0 application client secret
# ⚠️ CRITICAL SECURITY WARNING:
#   - NEVER commit this value to git or expose it publicly
#   - Set it ONLY in Railway environment variables or local .env file
#   - If accidentally exposed, rotate it immediately in Auth0 Dashboard:
#     Applications → [Your App] → Settings → Rotate Client Secret
#   - Contact project admin for the current secret value
AUTH0_CLIENT_SECRET=REPLACE_WITH_YOUR_AUTH0_CLIENT_SECRET

# ⚠️ IMPORTANT: Auth0 Application Settings
# In your Auth0 Dashboard, configure the following URLs for your application:
# (Applications → [Your App Name] → Settings)
#
# For Local Development:
#   - Allowed Callback URLs: http://localhost:5000/api/callback
#   - Allowed Logout URLs: http://localhost:5000
#   - Allowed Web Origins: http://localhost:5000
#
# For Railway Deployment:
#   - Allowed Callback URLs: https://your-app.railway.app/api/callback
#   - Allowed Logout URLs: https://your-app.railway.app
#   - Allowed Web Origins: https://your-app.railway.app
#
# Replace 'your-app' with your actual Railway app subdomain.

# Optional: Session Configuration
# If not set, AUTH0_SECRET will be used for session encryption
# SESSION_SECRET=your-session-secret-here

# Optional: AI Features
# OpenAI API key for AI-powered trading features
# AI_INTEGRATIONS_OPENAI_API_KEY=sk-your-openai-api-key
# Custom OpenAI API base URL (optional)
# AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Server Configuration
# Server port (default: 5000)
# PORT=5000
# Node environment (development or production)
# NODE_ENV=development

# Optional: Exchange API Keys (for live trading)
# Coinbase Pro API credentials
# COINBASE_API_KEY=your-coinbase-api-key
# COINBASE_API_SECRET=your-coinbase-api-secret
# Kraken API credentials
# KRAKEN_API_KEY=your-kraken-api-key
# KRAKEN_API_SECRET=your-kraken-api-secret
